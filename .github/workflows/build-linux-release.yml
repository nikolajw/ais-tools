name: Build Linux Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0'

      - name: Restore dependencies
        run: |
          dotnet restore src/AisStreamer/AisStreamer.csproj
          dotnet restore src/AisLoader/AisLoader.csproj
          dotnet restore src/AisSimulator/AisSimulator.csproj

      - name: Build project
        run: |
          dotnet build src/AisStreamer/AisStreamer.csproj --configuration Release --no-restore
          dotnet build src/AisLoader/AisLoader.csproj --configuration Release --no-restore
          dotnet build src/AisSimulator/AisSimulator.csproj --configuration Release --no-restore

      - name: Publish for Linux
        run: |
          dotnet publish src/AisStreamer/AisStreamer.csproj --configuration Release --runtime linux-x64 --self-contained --output ./publish/linux-x64
          dotnet publish src/AisLoader/AisLoader.csproj --configuration Release --runtime linux-x64 --self-contained --output ./publish/linux-x64
          dotnet publish src/AisSimulator/AisSimulator.csproj --configuration Release --runtime linux-x64 --self-contained --output ./publish/linux-x64

      - name: Create archives
        run: |
          cd publish
          tar -czf aistools-linux-x64.tar.gz linux-x64/
          ls -lh *.tar.gz

      - name: Get version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION="${{ github.ref_name }}"
            VERSION=${VERSION#v}  # Remove 'v' prefix if present
          else
            VERSION=$(date +'%Y.%m.%d')-$(git rev-parse --short HEAD)
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: ncipollo/release-action@v1
        with:
          artifacts: "./publish/aistools-linux-x64.tar.gz"
          draft: false
          prerelease: false
          name: Release ${{ steps.version.outputs.version }}
          tag: v${{ steps.version.outputs.version }}
          allowUpdates: true
          body: "Linux x64 build - AisTools suite"
          token: ${{ github.token }}
